class Worker:

    def _init_(self, name, last_name, age):
        self.name = name
        self.last_name = last_name
        self.age = int(age)


    def getWorker(str):

        str.name = ["Katarzyna", "Lukasz", "Mateusz"]
        str.last_name = ["Kowalska", "Lipa", "Wieczorek"]
        str.age = [27, 38, 42]


        #Worker[1] =
    #@classmethod

    #i = 0
    #while i < len(self.name):
    #    print(self.name[i], self.last_name[i], self.age)
    #    i += 1

def main():
  
   
   